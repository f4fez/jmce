# Set the default behavior, in case people don't have core.autocrlf set.
* text=auto

# Batch files must have Windows file endings to work
*.bat text eol=crlf

# These are better left untouched
*.ses -diff
